name: Build
on: [workflow_dispatch]
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      JEKYLL_ENV: production
    steps:
      - uses: actions/checkout@v5
        with:
          repository: neveler/HMCL-docs
          ref: dev-i18n
          fetch-depth: 1
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.4'
          bundler-cache: true
      - name: build
        run: |
          echo "url: https://hmcl-docs.github.io" > _config_action.yml
          echo "footer:" >> _config_action.yml
          echo "  links:" >> _config_action.yml
          echo "    - label: Github" >> _config_action.yml
          echo "      icon: fab fa-fw fa-github" >> _config_action.yml
          echo "      url: https://github.com/HMCL-dev/HMCL" >> _config_action.yml
          bash _build/setup.sh _config_action.yml
          cd _site
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git init && git add .
          git commit -m "Deploy [skip ci]" || echo "No changes to commit"
          git checkout -b site
          git remote add origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
          git push origin site -f
